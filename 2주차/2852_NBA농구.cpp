#include <bits/stdc++.h>
using namespace std;

int n;
int tn;
string t;
int a[102];
string b[102];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> tn >> t;
        }
}